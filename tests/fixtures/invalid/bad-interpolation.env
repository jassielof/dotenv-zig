# Valid
APP_NAME=my-service
VERSION=1.0.0

# Malformed variable interpolation
UNCLOSED_BRACE="${DATABASE_HOST
EMPTY_VAR="${}"
NESTED_INVALID="${${INNER}}"
DOLLAR_NO_BRACE="value $ without brace"
SELF_REF="${SELF_REF}"
MIXED="${VALID} and ${UNCLOSED"

# Unreachable valid entry
FALLBACK=default
