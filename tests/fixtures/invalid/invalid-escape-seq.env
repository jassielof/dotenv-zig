# Starts valid
APP_ENV=production
BASE_URL=https://example.com

# Bad escape sequences inside quoted strings
GREETING="Hello \q World"
PATH_LIKE="C:\Users\johndoe\q\documents"
MSG='It's a test with \z escape'
FORMULA="e=mc\2"
WEIRD="value with \a \b \q \z escapes"

LOG_PATH=/var/log/app.log
